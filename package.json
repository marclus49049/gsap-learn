{
  "name": "gsap",
  "version": "1.0.0",
  "description": "This repo is just me learing the basics of gsap and also experimenting with it.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scss":"node-sass --watch scss -o css"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^5.0.0"
  }
}
